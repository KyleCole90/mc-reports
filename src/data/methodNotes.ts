/** Caveats that more than one report's method popover repeats. */

export const PRIORITY_NOTE =
  'The priority flag on a standard. The API has no priority field on objectives, so the sample sets it by hand.'

export const PAIR_SAMPLE_NOTE =
  'This fixture seeds each tracker-standard score directly and draws the item rows around it, so averaging the rows here will not land on the same figure. A live build averages the item analysis rows.'
